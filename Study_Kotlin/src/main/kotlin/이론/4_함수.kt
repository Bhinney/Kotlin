package 이론

fun main() {
    println(add(4, 3))
}

class `4_함수` {
    /*
     * 함수 : 특정한 동작을 하거나 원하는 결과값을 연산하는 데 사용
     * 단일 표현식 함수 (Single-Expression Function) :
     */
}

/* 더하기 연산 함수 만들기 by 단일 표현식 함수 */
fun add(a : Int, b : Int) = a + b /* 반환값 생략 가능 -> 추론해주기 때문 */